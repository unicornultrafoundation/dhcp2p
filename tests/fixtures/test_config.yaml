database:
  host: localhost
  port: 5432
  name: dhcp2p_test
  user: test_user
  password: test_password
  ssl_mode: disable

redis:
  host: localhost
  port: 6379
  password: ""
  db: 1

app:
  max_lease_retries: 3
  lease_retry_delay: 100
  lease_ttl: 3600
  nonce_ttl: 300

server:
  port: 8088
  host: "0.0.0.0"

logging:
  level: debug
  format: json
